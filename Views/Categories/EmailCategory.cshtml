@model AddressBook.Models.ViewModels.EmailCategoryViewModel


<h1>Email Group</h1>
<div class="row-cols-sm-1 d-flex justify-content-center">
    <div class="col-md-12 col">
        <div class="glass-cf-style darkGlass p-2">
            <form asp-action="EmailCategory" method="post">
                <input type="hidden" asp-for="@Model.EmailData.GroupName" />
                <div class="row row-cols-1 row-cols-md-2 g-3 mb-3">
                    <div class="col col-md-12">
                        <label asp-for="@Model.EmailData.EmailAddress" class="form-label"></label>
                        <textarea type="email" readonly class="form-control" asp-for="@Model.EmailData.EmailAddress" rows="3"></textarea>
                    </div>
                </div>
                <div class="row row-cols-1 g-3">
                    <div class="col">
                        <label asp-for="@Model.EmailData.Subject" class="form-label"></label>
                        <input type="text" class="form-control" asp-for="@Model.EmailData.Subject" />
                    </div>
                    <div class="col">
                        <label asp-for="@Model.EmailData.Body" class="form-label"></label>
                        <textarea class="form-control" rows=10 asp-for="@Model.EmailData.Body"></textarea>
                    </div>
                    <div class="col">
                        <button class="btn btn-primary w-100" type="submit">Send</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>
<div>
    <a asp-action="Index">Back to List</a>
</div>